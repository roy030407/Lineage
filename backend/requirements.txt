# Generated from pyproject.toml's [project.dependencies], pinned to the
# versions this app is actually developed and tested against (pyproject.toml
# itself stays unpinned for local dev; this file trades that flexibility for
# deployment reproducibility). Regenerate by hand if pyproject.toml's
# dependency list changes -- there's no automated sync between the two.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
numpy==2.5.2
pandas==3.0.5
pyyaml==6.0.3
xgboost==2.1.3
scikit-learn==1.9.0
